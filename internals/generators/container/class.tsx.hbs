/**
*
* {{properCase name }}
*
*/

import React from 'react';
import { FormattedHTMLMessage } from 'react-intl';
import connectToState from 'utils/helperFunctions/connectToState';
import NestedApiCallPropTypes from 'utils/types/NestedApiCallPropTypes';
import messages from './messages';
import * as apiCalls from './apiCalls';

const container = '{{ camelCase name }}';

const {{ properCase name }} = ({ getConfig }: Props) => {
console.log(getConfig);
return (
<div>
  <FormattedHTMLMessage {...messages.header} />
</div>
);
}

interface Props {
getConfig: NestedApiCallPropTypes;
}

export default connectToState({
container,
apiCalls,
})({{ properCase name }})